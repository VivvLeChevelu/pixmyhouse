{% extends 'base.html.twig' %}

{% block title %}DetailCommandes{% endblock %}

{% block body %}
    <h1 class="text-center mb-4 bg-secondary">DetailCommandes</h1>

    <table class="table table-dark table-striped table-sm table-responsive-md">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail_commande.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ detail_commande.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ detail_commande.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('detail_commandes_index') }}" class="btn btn-primary">Retour à la liste</a>

    <div> <a href="{{ path('detail_commandes_edit', {'id': detail_commande.id}) }}" class="btn btn-primary mt-2 mb-2">Edition</a> </div>

    <div>{{ include('detail_commandes/_delete_form.html.twig') }} <div>
{% endblock %}