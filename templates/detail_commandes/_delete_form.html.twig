<form method="post" action="{{ path('detail_commandes_delete', {'id': detail_commande.id}) }}" onsubmit="return confirm('Etes vous sûre de vouloir supprimer cette commande?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detail_commande.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>