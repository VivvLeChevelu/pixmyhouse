{% extends 'baseAdmin.html.twig' %}

{% block title %}New Commande{% endblock %}

{% block body %}
<div class="admincss">
    <h1 class="text-center mb-4 bg-secondary" >Nouvelle commande</h1>

    {{ include('commande/_form.html.twig') }}

    <a href="{{ path('commande_index') }}" class="btn btn-primary mt-2">Retour à la liste</a>
</div>
{% endblock %}