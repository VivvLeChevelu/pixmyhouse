{% extends 'baseAdmin.html.twig' %}

{% block title %}Edition Utilisateur{% endblock %}

{% block body %}
<div class="usercss">
    <h1 class="text-center mb-4 bg-secondary">Edition utilisateur</h1>
    
    {{ include('user/_form.html.twig', {'button_label': 'Enregistrer'}) }}       
    <a href="{{ path('user_index') }}"  class="btn btn-primary">Retour à la liste des utilisateurs</a>
    <div class="mt-2"> {{ include('user/_delete_form.html.twig' ) }}   </div>
</div>
{% endblock %}